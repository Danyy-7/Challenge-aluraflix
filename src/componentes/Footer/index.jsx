import styled from './Footer.module.css'

const Footer = () =>{
    return(

        <footer className={styled.contenedor}>
            <img className={styled.logo} src="/img/logo-sin-fondo.png" alt="logo"/>
        </footer>
    )
    
    
}

export default Footer